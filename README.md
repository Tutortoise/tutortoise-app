# Tutortoise Mobile App

## Setup

1. Create or edit `local.properties` file in the root project directory
2. Configure the API base URL:
    ```properties
    base.url=https://tutortoise-api.app/api/v1/
    ```
